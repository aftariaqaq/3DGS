# Legacy Scripts

These scripts are retained as historical references from the CPU/OpenSplat prototype. They are not part of the supported CUDA-only workflow.

Use `scripts/ops` for active operations.
